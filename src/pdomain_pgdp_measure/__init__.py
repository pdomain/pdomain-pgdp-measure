"""Font-free measurement of PGDP scans.

Task 2 of the extraction plan fills this package with the modules moved from
``pdomain_ocr_synth.pgdp``. Until then it exists only so the distribution is a
real package rather than an implicit namespace package.
"""
